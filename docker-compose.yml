version: '3.8'

services:
  app:
    build: .
    container_name: sph-app
    restart: unless-stopped
    ports:
      - "8000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
